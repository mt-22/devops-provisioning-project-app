name: Deploy Web App

on:
  push:
    branches:
      - master

permissions:
  security-events: write

jobs:
  build:
    name: Build and Scan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # Initializes the CodeQL tools for scanning.
      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: javascript 

      # Runs the actual analysis
      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3
      
      # Scan for hardcoded secrets
      - name: Perform Secret Scan
        uses: gitleaks/gitleaks-action@v2
        with:
          # This tells Gitleaks to output its findings in a format
          # that GitHub can display on the security tab.
          report_format: sarif
          report_path: gitleaks-report.sarif

      # This step uploads the Gitleaks report so you can see it
      - name: Upload Gitleaks report
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: gitleaks-report.sarif

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Configure SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.EC2_SSH_KEY }}

      - name: Deploy to EC2
        run: |
          ssh -o StrictHostKeyChecking=no ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }} '

            echo "Connected to EC2. Pulling latest from source..."
            cd /tmp/devops-app || git clone https://github.com/mt-22/devops-provisioning-project-app.git /tmp/devops-app

            cd /tmp/devops-app

            git pull origin master

            echo "Deploying app..."
            sudo cp /tmp/devops-app/index.html /var/www/html/index.html

            echo "Deployment complete" 
          '
